# 长期记忆

## 工作规则
- worklog/ 按需自建：时间命名文件 + WORKLOG.md 索引短期重点；已完结事项移出索引但保留原文件。
- COMMUNICATIONS.md 只记录新对齐事项（设计/实现/执行对齐），不记流水。
- MEMORY.md 只记跨会话长期稳定规则，不记项目阶段/进度等会变状态。
- 设计对齐规则：未讨论清楚前禁止直接给出整体方案。先说"要做什么"，再说"怎么实现"，逐个局部讨论清楚后，再按各局部方案拼出整体方案。
- 设计顺序规则：先定生产者接口再设计消费者。消费者依赖生产者的输出契约，接口未定就设计消费者是本末倒置。
- 数据泄漏规则：训练/测试分割必须按日期明确切割，禁止按行数比例切割；训练集末尾与测试集开头之间必须留出足够间隔（至少max_days个交易日），防止标签计算的前瞻泄漏。
- 迁移流程：读完代码→理解运行逻辑→保证逻辑一致→按项目设计原则放置代码。
- 测试集规则：测试集必须是最近的时间段，不是历史中间段；训练集用更早的数据。
- 职责分离规则：纯技术指标（indicators）和交易规则（trade_label）必须分文件存放，不得混装。
- 流程规则：设计对齐未完成禁止动手写文件；无授权不改动；读代码必须读完所有文件再下结论。
