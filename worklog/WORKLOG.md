# WORKLOG（短期记忆）

> 只留当前未决重点。已完成事项详情见 `worklog/2026-07-archived.md`（保留备查）。

## 当前未决（待领导拍板）

1. **第一版 A 股标的范围**：仅股票 / +ETF / +ETF+可转债(建议) / 全可买含逆回购。
2. **Tushare 积分等级**：分钟 `stk_mins` 需 ≥5000，决定可拉取的表（决定是否走 tushare 还是 baostock 兜底）。
3. **成本模型参数**：初始资金 / 佣金率 / 印花税 / 滑点 / T+1（回测模块要用）。
4. **序列模型本机路径**：torch 已就位，待读后定训练模块注入契约。

> 频率与数据源已定：分钟 1min + 日线双粒度，baostock/tushare + duckdb，预留 BTC。数据模块已落地（双面：查询面按需回源 + 同步面可选预热）。
