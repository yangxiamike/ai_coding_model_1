# exp_hs300 实验报告
**时间**: 2026-07-15 13:16
**脚本**: `exp_hs300.py` (未修改)
**运行耗时**: ~2秒 (50 epoch)

---

## 一、核心问题：训练未实际执行

**DataLoader 返回 0 batch，导致 50 个 epoch 全部为空跑。**

### 数据流水线瓶颈分析

| 阶段 | 训练集 | 测试集 |
|------|--------|--------|
| 原始数据 (provider.hour) | 342 行 | 216 行 |
| 技术指标后 (compute_all_kline_indicators + calculate_rates) | 142 行 | 16 行 |
| 合并标签后 (merge target) | 123 行 | **0 行** |
| 有效样本数 (seq_len=120) | 4 个 | 0 个 |
| DataLoader (batch=64, drop_last=True) | **0 batch** | **0 batch** |

### 根因

1. **原始数据量严重不足**: 2023-01-01 ~ 2026-04-01 共3年+，期望应有约5000条小时线，实际仅342条（约缺93%）。怀疑 `provider.hour()` 有数据拉取限制。

2. **指标计算吞噬数据**: `compute_all_kline_indicators` 含大量滚动窗口指标（如 MAX_120 等），将342行砍为142行（丢掉200行）。

3. **标签计算进一步削减**: `compute_trailing_stop_target(max_days=3)` 因前向窗口再砍掉尾部数据。

4. **序列长度过滤**: `LEN=120` 将123行训练数据压缩至仅4个有效序列样本。

5. **测试集完全消失**: 216行原始数据经指标→标签→序列过滤全部耗尽，结果为0。

---

## 二、标签失衡

`正样本比例: 0.000` — 训练集所有标签均为0。即使有数据可训练，BCEWithLogitsLoss 在单类别上也无效。

---

## 三、建议排查方向（供后续参考，本次不改代码）

1. 验证 `provider.hour("510300.SH", ...)` 是否完整返回所有小时线
2. 评估指标计算的最小所需窗口，确认数据量是否匹配
3. 考虑将 `batch_size` 改为1，或在数据集小于 batch_size 时不使用 `drop_last`
4. 考虑调整 `trailing_pct` / `profit_threshold` 参数以产生正标签
5. 验证 `GAP_DAYS=5` 是否足够（TRAIN_END=20260401, TEST_START=20260408，仅5个自然日）
